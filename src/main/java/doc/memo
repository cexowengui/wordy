1.主线程退出子线程会不会退出？
无论是java还是python或者C，主线程的结束并不会导致子线程结束，而是会等待所有子线程执行完进程退出。但是在网上搜索到的大部分例子，
一般都是那个c语言的例子，main函数里面用pthread_create创建一个子线程，然后在子线程里面sleep几秒钟后打印东西，结果会发现，
main函数结束后子线程并没有打印东西，得出结论是主线程结束由其创建的子线程都会结束。但是在java里面写小例子测试，会发现主线程结束，
子线程还是可以继续运行。于是还有人得出主线程子线程的退出关系由语言决定。这都是错误的。正确的结论是进程结束会导致所有的线程结束，而线程
之间都是平等的，没有父线程子线程一说，只是习惯这么称呼。目前的linux系统线程模型中，线程都是一样对待，都是归属于进程管理。所以父线程
退出后子线程毫不影响。那么C语言那个例子的原因是main函数所在的主线程退出了导致进程退出，所以表现出那样的结论，但是其实是有系统函数让这个
进程不退出的。而java的那个结论是因为主线程结束但是JVM会检测到还有线程存活于是会继续运行，等子线程运行结束JVM才会退出。

2.主进程退出子进程会不会退出？
3.